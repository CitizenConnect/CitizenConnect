@model IEnumerable<CitizenConnect.Models.ReportType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReportTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReportTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReportTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReportTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReportTypeID })
        </td>
    </tr>
}

</table>
